server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/staleks/config-server-properties

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
